# Variáveis de ambiente para o projeto
# Crie um banco PostgreSQL (ex: Supabase, Neon, ou local) e cole a URL aqui
DATABASE_URL="postgresql://usuario:senha@localhost:5432/parlamento_jovem?schema=public"

# Chave secreta para JWT (Mude em produção)
JWT_SECRET="chave-secreta-parlamento-jovem-mococa-2026"
